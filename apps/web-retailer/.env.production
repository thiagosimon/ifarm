# ─── Web Retailer Production Environment ─────────────────────────────────────
# Public vars (embedded in JS bundle)
NEXT_PUBLIC_API_URL=https://api.ifarm.agr.br/api

# Server-side auth config
AUTH_SERVICE_URL=https://api.ifarm.agr.br/api
AUTH_TRUST_HOST=true

# NEXTAUTH_SECRET must be set in Netlify/Vercel dashboard (never commit secrets)
