import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('iFarm app smoke test', (WidgetTester tester) async {
    // Placeholder test — full integration tests use real API providers
    expect(true, isTrue);
  });
}
